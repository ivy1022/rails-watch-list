<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://pbs.twimg.com/media/DmBaHWzUYAA4sIS.jpg);">
  <div class="container">
    <h1>What's your favorite <strong>movie?</strong></h1>
    <p>🎞 🎫 🎬 📽 🍿</p>
    <%= link_to 'Create a movie list', new_list_path, class: 'btn btn-flat' %>
  </div>
</div>

<h1>Movie list</h1>



<div class="cards">
  <% @lists.each do |list| %>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= list.photo.attached? ? cl_image_path(list.photo.key, height: 300, crop: :fill) : ""; %>')">
    <%= link_to list.name, list_path(list) %>
  </div>
  <% end %>
</div>


<%#= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>
